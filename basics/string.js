const name = 'hitesh';
const repoCount = 50;

console.log(name +repoCount)

const gameName = new String('hitesh');
//console.log(`${gameName} ${repoCount} ${gameName}` )


//some methods of string
console.log(gameName[1]);
console.log(gameName.length);